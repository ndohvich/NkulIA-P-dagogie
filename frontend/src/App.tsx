export function App() {
  return (
    <main className="min-h-screen bg-stone-50 text-slate-800">
      <section className="mx-auto max-w-3xl px-8 py-24">
        <p className="font-semibold tracking-widest text-gold">NKULIA · PHASE 3</p>
        <h1 className="mt-3 text-4xl font-bold text-forest-deep">
          Fondations en place
        </h1>
        <p className="mt-5 text-lg text-slate-600">
          Authentification locale et gestion du profil connectées à l'API
          (voir docs/ISSUES.md #5–#8). Les écrans métier suivants — import,
          sélection pédagogique, génération — arrivent par pull request,
          dans l'ordre défini dans docs/PULL_REQUESTS.md.
        </p>
      </section>
    </main>
  );
}
